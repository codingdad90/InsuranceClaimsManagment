@model IEnumerable<MAKClaims.Models.Claim>

@{

    // ViewBag.Title = "AdjustorView";
}

<style>
    .table {
        border-collapse: collapse;
    }

        .table th,
        .table td {
            padding: 10px;
        }

        .table th {
            background: #f4f6f9;
        }

        .table.sticky th {
            position: sticky;
            top: 0;
        }
</style>



<h2>@ViewBag.Name</h2>

@*@Html.ActionLink("Create New", "Create")*@



<!--<div align="left">-->
    @*<div class="container">*@
    <!--<div class="form-group">
        <div class="input-group">
            <span class="input-group-text"></span>
            <input type="text" id="Search" placeholder="Search" class="form-control" />
        </div>
    </div>-->
    @*</div>*@
<!--</div>-->

@*<input type="text" id="txtsearch" placeholder="Enter address info" />
    <hr/>*@


<br />

<table class="table sticky">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClaimId)
            </th>

            <th>
                @Html.ActionLink(Html.DisplayNameFor(model => model.Reserves).ToString(), "AdjustorView", new { sortOrder = ViewBag.ReserveSort })
            </th>
            <th>
                @Html.ActionLink(Html.DisplayNameFor(model => model.Deductable).ToString(), "AdjustorView", new { sortOrder = ViewBag.DeductableSort })
            </th>
            <th>
                @Html.ActionLink(Html.DisplayNameFor(model => model.AmountPaid).ToString(), "AdjustorView", new { sortOrder = ViewBag.AmountPaidSort })
            </th>
            <th>
                @Html.ActionLink(Html.DisplayNameFor(model => model.Status).ToString(), "AdjustorView", new { sortOrder = ViewBag.StatusActive })
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Property.Address)
            </th>
            <th>
                @Html.ActionLink(Html.DisplayNameFor(model => model.DateOfLoss).ToString(), "AdjustorView", new { sortOrder = ViewBag.DateLossSort })
            </th>
            <th>Claim Admin</th>

            <th></th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr class="Search">
                <td>
                    @Html.ActionLink(item.ClaimId.ToString(), "ClaimDetails", "Manualviews", new { id = item.ClaimId }, null)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.Reserves)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Deductable)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AmountPaid)
                </td>
                <td>

                    @Html.DisplayFor(modelItem => item.Status)

                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Property.Address)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.DateOfLoss)
                </td>

                <td>
                    @Html.ActionLink("Edit Claim", "EditClaim", new { id = item.ClaimId }) 
                    @* @Html.ActionLink("Details", "Details", new { id=item.ClaimId }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.ClaimId })*@
                </td>
</tr>
        }
    </tbody>
</table>

<script src="~/Scripts/jquery-3.5.1.min.js"></script>

<script>
    $(document).ready(function () {
        function Contains(text_one, text_two) {
            if (text_one.indexof(text_two) != -1)
                return true;
        }
        $("#Search").keyup(function () {
            var searchText = $("#Search").val().toLowerCase();
            $(".Search").each(function () {
                if (!Contains($(this).text().toLowerCase(), searchText)) {
                    $(this).hide();
                }
                else {
                    $(this).show();
                }
            });
        });
    });
</script>





@*<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        $("#txtsearch").on("keyup", function () {
            var textenter = $(this).val();
            $("table tr").each(function (results) {
                if (results !== 0) {
                    var id = $(this).find("td:nth-child(5)").text();
                    if (id.indexof(txtenter) !== 0 && id.toLowerCase().indexof(txtenter.toLowerCase()) < 0) {
                        $(this).hide();
                    }
                    else
                    {
                        $(this).show();
                    }
                }
            })
        })
    </script>*@


@*<script >

    $("#checkbox-container :checkbox").on("change", function(){
      alert("The checkbox with the ID '" + this.id + "' changed");
    });
    </script>*@
@*<script>
     function GetStatus(elem) {
        var status= $(elem).is(':checked');
        $.ajax({
            type: 'POST',
            url: "@Url.Action("AdjustorView", "Manager")",
            data: { check: status},
            success: function(res) {
                console.log(res);
            },
            dataType: 'json'
        });
    }</script>*@

