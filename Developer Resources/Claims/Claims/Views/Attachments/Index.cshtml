@model IEnumerable<Claims.Models.Attachment>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Attachment1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClaimAction.Note)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Attachment1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClaimAction.Note)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.AttachmentId }) |
                @Html.ActionLink("Details", "Details", new { id=item.AttachmentId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.AttachmentId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
